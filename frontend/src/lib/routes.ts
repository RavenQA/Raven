import BrowsersPage from "$lib/components/pages/browsers.svelte";
import SessionsPage from "$lib/components/pages/sessions.svelte";

export const Routes = Object.freeze({
  Browsers: BrowsersPage,
  Sessions: SessionsPage,
});
